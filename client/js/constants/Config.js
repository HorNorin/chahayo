let domain = `http://api.${location.host}`;

const Config = {
  USER: `${domain}/user`,
  LOGIN: `${domain}/login`,
  LOGOUT: `${domain}/logout`,
  REGISTER: `${domain}/user`
};

export default Config;
