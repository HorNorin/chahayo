let domain = `http://api.${location.host}`;

export const Config = {
  USER_URL: `${domain}/user`,
  LOGIN_URL: `${domain}/login`,
  LOGOUT_URL: `${domain}/logout`,
  REGISTER_URL: `${domain}/user`,
  BOARD_URL: `${domain}/boards`
};
